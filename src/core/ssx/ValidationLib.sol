library ValidationLib {
    function enforceRecipient(
        uint8 configBitmap,
        bytes32 configId,
        address sponsor,
        ChainSpecificConfig storage config
    )
        internal
        returns (bool)
    { }
}
