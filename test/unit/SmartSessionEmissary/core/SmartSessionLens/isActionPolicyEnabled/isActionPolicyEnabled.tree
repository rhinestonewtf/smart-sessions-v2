SmartSessionLens::isActionPolicyEnabled
├── when no session exists
│   └── it returns false
├── when checking different policy
│   └── it returns false
└── when policy is enabled
    └── it returns true