SmartSessionLens::isActionIdEnabled
├── when no session exists
│   └── it returns false
├── when checking different action id
│   └── it returns false
└── when action id is enabled
    └── it returns true