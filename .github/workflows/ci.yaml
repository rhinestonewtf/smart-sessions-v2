on:
  workflow_dispatch:
  push:
    branches:
      - "main"
  pull_request:

jobs:
  lint:
    uses: "rhinestonewtf/reusable-workflows/.github/workflows/forge-lint.yaml@477facdd655676cbf7aa47b799959080c71bc64b"
    secrets:
      DEPLOY_KEYS: ${{ secrets.DEPLOY_KEYS }}

  build:
    uses: "rhinestonewtf/reusable-workflows/.github/workflows/forge-build.yaml@477facdd655676cbf7aa47b799959080c71bc64b"
    secrets:
      DEPLOY_KEYS: ${{ secrets.DEPLOY_KEYS }}

  test:
    needs: ["lint", "build"]
    uses: "rhinestonewtf/reusable-workflows/.github/workflows/forge-test.yaml@477facdd655676cbf7aa47b799959080c71bc64b"
    with:
      foundry-fuzz-runs: 5000
      foundry-profile: "test"
      match-path: "test/**/*.sol"
